import * as React from 'react';

const Home: React.FC<{}> = () => {

   

   return (
      <div className="home-wrapper">
         Home Component
      </div>
    );
}

export default React.memo(Home);