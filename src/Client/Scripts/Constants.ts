//Define constants here
const constants = {
    routes: {
        home: '/',
        recipes: '/recipes',
        recipe: '/recipe',
        register: '/register'
    }
};
export default Object.freeze(constants);